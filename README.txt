Things I did:

<-------------Command line-------->
npm install sass
npm install bootstrap
npm install postcss-cli
npm install autoprefixer
npm install node-sass

#This will activate the watcher
npm run scss  (Add to serve script with && start npm run scss on to the run serve)
